---
import BaseLayout from "@/layouts/BaseLayout.astro";
import { getCollection } from "astro:content";

const notes = (await getCollection("blog", ({ data }) => !data.draft))
  .sort((a, b) => b.data.date.getTime() - a.data.date.getTime())
  .slice(0, 3);

const methodItems = [
  {
    title: "Source-aware",
    detail: "Public information is shown with source context where available.",
  },
  {
    title: "Time-aware",
    detail: "Charts keep observation and retrieval dates visible.",
  },
  {
    title: "Mock-first",
    detail: "Prototype data is used until source rules are reviewed.",
  },
  {
    title: "Careful claims",
    detail: "No dataset is described as live or complete without support.",
  },
] as const;
---

<BaseLayout
  title="Track & Tote"
  description="Careful notes and early data experiments around sustainability reporting, ESG, GHG and OHSE."
>
  <main>
    <section class="home-hero">
      <img
        src="https://images.unsplash.com/photo-1450101499163-c8848c66ca85?auto=format&fit=crop&w=1800&q=80"
        alt="Printed reports, notebooks, and research material arranged for review"
        loading="eager"
      />
      <div class="home-hero-content">
        <p class="kicker">Public notes and data experiments</p>
        <h1>Track &amp; Tote</h1>
        <p class="home-hero-subtitle">
          Careful notes and early data experiments around sustainability reporting, ESG, GHG and
          OHSE.
        </p>
        <p>
          This site is a working space for small articles, method notes and public-data prototypes.
          The current ESG/OHSE project is intentionally mock-first while sources, licences,
          attribution and refresh rules are reviewed.
        </p>
      </div>
    </section>

    <section class="section-block">
      <div class="section-head">
        <p class="kicker">Featured project</p>
        <h2>India ESG/OHSE Public Data Project</h2>
      </div>
      <article class="feature-card">
        <p class="status-label">Mock-data prototype</p>
        <p>
          An early-stage project exploring how public datasets can support sustainability, safety
          and reporting context over time.
        </p>
        <a class="button" href="/projects/india-esg-ohse">View project</a>
      </article>
    </section>

    <section class="section-block">
      <div class="section-head">
        <p class="kicker">Latest notes</p>
        <h2>Recent writing</h2>
      </div>
      <div class="notes-grid">
        {
          notes.map((note) => (
            <article class="note-card">
              <h3>
                <a href={`/notes/${note.slug}/`}>{note.data.title}</a>
              </h3>
              <p>{note.data.description}</p>
              <div class="note-meta">
                <time datetime={note.data.date.toISOString()}>
                  {note.data.date.toLocaleDateString("en-IN", {
                    year: "numeric",
                    month: "short",
                    day: "2-digit",
                  })}
                </time>
              </div>
            </article>
          ))
        }
      </div>
      <p class="subtle-link">
        <a href="/notes">Browse all notes</a>
      </p>
    </section>

    <section class="section-block">
      <div class="section-head">
        <p class="kicker">Method strip</p>
        <h2>How we keep the work disciplined</h2>
      </div>
      <div class="method-strip">
        {
          methodItems.map((item) => (
            <article>
              <h3>{item.title}</h3>
              <p>{item.detail}</p>
            </article>
          ))
        }
      </div>
    </section>
  </main>
</BaseLayout>
